/**
 * @deprecated 이 파일의 내용은 `src/features/notifications/` 로 옮겨졌다.
 *
 * 알림 화면이 생기면서 목데이터의 정본이
 * `features/notifications/mocks/notification.mock.ts` 로 이동했다.
 * 남은 참조가 없으므로 이 파일은 삭제해도 된다.
 */
export type { AppNotification as NotificationPreview } from '@/src/features/notifications/types/notification';
export { appNotifications as notificationPreviews } from '@/src/features/notifications/mocks/notification.mock';
