import { ScreenPlaceholder } from '@/src/components/feedback/ScreenPlaceholder';

export default function PlaceListRoute() {
  return <ScreenPlaceholder description="동반 가능한 장소 목록 작업 영역" title="동반 장소" />;
}
