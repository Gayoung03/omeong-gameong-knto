"""여행기록 카드 생성 파이프라인.

사진 한 장을 받아 손글씨 메모가 얹힌 카드 PNG 를 만든다.

## 이 폴더는 DB·S3·라우트를 모른다

사진 바이트를 받아 PNG 바이트를 돌려주는 것까지다. 원본을 어디서 읽고 결과를 어디에
올릴지는 `integrations/llm/travel_log_image.py` 가 정한다(2026-09-14 연결).
그래서 이 폴더는 요금이 드는 확인을 서버 없이 할 수 있다 —
`scripts/travel_card_smoke.py` 에 로컬 파일 하나를 넣으면 된다.

## 다섯 단계

    vision       사진 분석 + 안전 판정      (LLM)
    caption      메모 4~6개 생성            (LLM)
    prompts      이미지 프롬프트 조립       (문자열 합치기, 호출 없음)
    image_edit   이미지 편집 호출           (LLM, 전체 지연의 대부분)
    card_render  손글씨만 뽑아 원본에 합성  (Pillow, 호출 없음)

`agent.build_card()` 가 다섯을 순서대로 부른다. 부르는 쪽은 이 함수 하나만 알면 된다.

마지막 단계가 있는 이유는 `images.edit` 가 **모든 픽셀을 새로 그리기 때문이다.**
생성물을 그대로 내보내면 얼굴이 바뀐 사진이 기록으로 남는다. `card_render` 를 볼 것.

## 예외를 던지지 않는다

실패도 결과다. `CardResult` 하나로 네 가지 결말을 구분해 돌려준다
(`types.CardOutcome`). 프론트가 분기 하나로 처리할 수 있게 하려는 것이다.
"""
