"""단계 3 — 이미지 편집 프롬프트 조립.

**LLM 을 부르지 않는다.** 문자열을 합칠 뿐이다. 그래서 이 단계는 공짜이고, 실패하지
않고, 테스트가 쉽다. 프롬프트를 고칠 때 모델 호출 없이 결과를 눈으로 확인할 수 있다.

세 조각으로 나눈다.

    공통 스타일   그림체·펜·장식 규칙 (말투마다 조금 다름)
    분류별 내용   kind 마다 무엇을 짚을지
    가드          하지 말 것. 이 기능의 최대 난제가 여기 있다

## 가드가 왜 제일 중요한가

이미지 모델은 한글을 조판하는 게 아니라 **그린다.** 그래서 "맛있어" 를 "맛있ㅓ" 로
그리는 실패가 실제로 자주 난다 — 자음이나 모음이 홀로 떨어진 모양이다. 한 글자만
깨져도 카드 전체가 못 쓰게 된다.

막는 방법이 둘이다. 하나는 **온전한 음절 블록으로만 그리라**고 못 박는 것, 다른 하나는
**자신 없으면 그 메모를 통째로 빼라**고 허락하는 것이다. 두 번째가 중요하다 —
반쪽짜리 한글을 억지로 그리는 것보다 메모 하나 적은 편이 낫다.
"""

from .types import CardText, PhotoKind, WritingStyle

# ---------------------------------------------------------------------------
# 공통 스타일
# ---------------------------------------------------------------------------

_STYLE_BASE = """{opening}

[그림체]
- 얇은 흰색 펜으로 직접 쓴 듯한 손글씨. 자연스럽고 살짝 삐뚤삐뚤하게.
- **피사체의 외곽선을 따라 테두리를 그리지 마.** 사람이나 동물의 몸을 따라
  흰 선을 두르면 안 된다. 손글씨와 화살표, 장식만 그린다.

[화살표 — 장식이 아니다]
- 화살표는 **메모에서 출발해, 그 메모가 가리키는 것에서 끝난다.** 그 외엔 그리지 마라.
- 출발점은 메모 글자 바로 옆에 붙는다. **허공에서 시작하면 안 된다.**
- 화살촉은 가리킬 대상 위에서 멈춘다. **대상을 지나쳐 빈 곳으로 빠지면 안 된다.**
- 아래 메모 목록에서 `(가리킬 것: ...)` 이 적힌 메모에만 화살표를 그린다.
  `(화살표 없음)` 이라고 적힌 메모에는 **절대 화살표를 그리지 마라.**
  날씨나 기분처럼 한 지점을 집을 수 없는 말에 화살표를 달면, 보는 사람은 화살표 끝에
  무엇이 있는지 찾다가 아무것도 없다는 것을 알게 된다.
- 한 메모에 화살표는 **하나까지.** 가리킬 메모가 없는 화살표는 하나도 없다.
- **점선으로, 완만하게 휜 곡선으로 그린다.** 자로 그은 듯한 곧은 실선은 쓰지 마라 —
  손으로 쓱 그은 느낌이어야 손글씨와 같은 결로 보인다.
- 길이는 짧게. 화면을 가로지르거나 크게 빙 돌지 마라.
- **대상 옆에 메모를 놓을 자리가 없으면 그 메모의 화살표는 그리지 마라.**
  멀리 있는 것까지 화살표를 길게 늘이면 중간에 엉뚱한 것들을 지나가고, 화살촉이
  대상에 못 닿은 채 허공에서 끝난다. 메모만 두는 편이 낫다.
- 가리킬 것이 사람이나 동물이 **아니라면, 화살촉이 사람이나 동물 위에 놓이면 안 된다.**
  풀을 가리키려다 사람 다리를 찍으면 보는 사람은 그 사람 이야기인 줄 안다.
- 가리킬 것이 사람이나 반려동물일 때도 **얼굴 위에서 끝내지 마라.** 몸 옆에서 멈춘다.

[메모를 어디에 놓을지 — 내용과 자리를 맞춰라]
- **각 메모는 그 메모가 말하는 대상 가까이에 놓는다.** 빈 곳이라고 아무 데나 두지 마라.
    하늘·구름 이야기 -> 사진 위쪽 하늘에
    바닥·잔디·모래·발밑 이야기 -> 사진 아래쪽에
    반려동물·사람 이야기 -> 그 대상 바로 옆에
    멀리 보이는 것 이야기 -> 그것이 보이는 쪽에
  하늘 이야기가 발밑에 놓이면 읽는 사람이 헷갈린다.
- 그런 다음 남은 여백에 고르게 나눠 놓는다. 한쪽에 몰리지 않게.

[글씨 두 종류로 구분]
- 제목 1개는 크고 굵게. {title_pen}
  {title_avoid}
- 나머지 메모는 전부 얇은 흰색 펜으로 작게. 제목보다 확실히 가늘게.
  굵은 글씨로 메모를 여러 개 달면 사진이 안 보인다.

[장식]
{decorations}
과하지 않게, 메모 사이사이에만 배치해줘."""

_OPENING_PHOTO = "이 사진 위에 손글씨 메모를 얹어 여행 일기 카드로 만들어줘."

#: 손글씨 레이어만 따로 받는 모드. 왜 이렇게까지 하는지는 card_render 를 볼 것 —
#: 사진과 손글씨가 섞여 나오면 어느 픽셀이 손글씨인지 **추측**해야 하고, 그 추측이
#: 흰 털·흰 운동화·나뭇잎 틈 하늘에서 계속 틀렸다(2026-09-12).
_OPENING_PLATE = """이 사진 위에 얹을 **손글씨 레이어**를 만들어줘.

**출력은 순수한 검은 배경(#000000)이어야 한다.** 사진에 있는 것 — 사람, 동물, 나무,
잔디, 하늘, 길 — 은 무엇도 그리지 마라. 전부 새까맣게 덮는다.
그 검은 배경 위에 **흰색 손글씨와 화살표, 장식만** 그린다.

사진은 **어디에 무엇을 쓸지 정하는 데만** 쓴다. 사람 얼굴이 있는 자리와 반려동물이
있는 자리는 비워 두고 그 옆 여백에 메모를 놓는다. 완성된 카드에서는 이 검은 배경이
원본 사진으로 바뀌므로, 자리가 맞아야 메모가 제자리에 얹힌다."""

_TITLE_PEN = {
    WritingStyle.JEJU_DIALECT: (
        "**굵은 붓으로 눌러쓴 한글 캘리그래피.** 붓끝이 갈라지고 획 두께가 "
        "들쭉날쭉하며 끝이 각지게. 손목에 힘을 준 듯 거칠고 시원시원하게."
    ),
    WritingStyle.DOG_DIARY: (
        "**동글동글하고 통통한 손글씨.** 획 두께가 처음부터 끝까지 일정하고 "
        "끝이 둥글게, 아이가 쓴 듯 삐뚤빼뚤하게."
    ),
}

#: 두 말투의 제목이 같은 글씨체로 나오는 일이 있어(2026-09-12) 반대쪽을 짚어 막는다.
_TITLE_AVOID = {
    WritingStyle.JEJU_DIALECT: (
        "동글동글하고 매끈한 글씨로 쓰지 마라 — 그건 다른 말투의 글씨다. "
        "획에 거친 붓 자국이 보여야 한다."
    ),
    WritingStyle.DOG_DIARY: (
        "거친 붓글씨로 쓰지 마라 — 그건 다른 말투의 글씨다. "
        "획이 매끈하고 둥글어야 한다."
    ),
}

_DECORATIONS = {
    WritingStyle.JEJU_DIALECT: (
        "작은 하트(♡), 반짝이(✨), 파도 물결선, 감귤 모양을 아주 작게."
    ),
    WritingStyle.DOG_DIARY: (
        "발바닥 자국, 뼈다귀, 작은 하트(♡), 반짝이(✨), 별(★), "
        "신나서 튀는 짧은 선을 아주 작게."
    ),
}

# ---------------------------------------------------------------------------
# 분류별 내용
# ---------------------------------------------------------------------------

_KIND_HINTS = {
    PhotoKind.PET_SOLO: (
        "반려동물이 주인공이다. 표정과 자세를 가리키는 메모를 가장 눈에 띄게 배치해줘."
    ),
    PhotoKind.PET_WITH_HUMAN: (
        "반려동물과 사람이 함께 있다. 메모는 반려동물 쪽에 더 많이 두고, "
        "사람의 얼굴은 글씨나 장식으로 가리지 마."
    ),
    PhotoKind.PET_WITH_PET: (
        "반려동물이 여럿이다. 각각을 가리키는 메모를 하나씩 두어 구분되게 해줘."
    ),
    PhotoKind.SCENERY: (
        "풍경 사진이다. 사진에 동물이 없으면 동물을 그려 넣지 마. "
        "하늘·바다·바위처럼 넓은 면에 메모를 얹어줘."
    ),
    PhotoKind.FOOD: "음식 사진이다. 각 그릇과 접시를 가리키는 메모를 하나씩 두어줘.",
    PhotoKind.OBJECT: "사물이 주인공이다. 그 사물을 가리키는 메모를 중심에 두어줘.",
    PhotoKind.OTHER: (
        "사진에서 가장 눈에 띄는 것부터 가리키는 메모를 배치해줘."
    ),
}

# ---------------------------------------------------------------------------
# 가드 — 이 기능의 최대 난제
# ---------------------------------------------------------------------------

_GUARD = """[반드시 지킬 것]
- 아래 메모 문장을 **글자 그대로** 써줘. 바꾸거나 새로 지어내지 마.
- **메모는 아래 목록의 {memo_count}개가 전부다. 한 줄도 더 만들지 마라.**
  여백이 남아도 메모를 새로 지어 채우지 마라. 남은 여백은 비워두거나
  하트·반짝임 같은 마크로만 채운다.
  지어낸 메모는 말투가 어긋나서 바로 티가 난다.
- 이 지시문에 나오는 설명 문장은 **그리라는 뜻이 아니다.** 카드에 쓸 글자는
  아래 [제목] 과 [메모] 의 `·` 뒤에 적힌 것뿐이다.
  **괄호 안의 `(가리킬 것: ...)` `(화살표 없음)` 은 화살표 지시일 뿐 글자가 아니다.
  카드에 그리지 마라.**
- **번호나 불릿을 그리지 마.** "1." "2." "-" 같은 기호를 글자로 쓰면 안 된다.
  목록에 붙은 번호는 순서를 알려주려고 적은 것이지 카드에 넣을 글자가 아니다.
- 모든 한글은 온전한 음절 블록으로만 그린다.
  자음이나 모음이 단독으로 보이면 안 된다.
  ("맛있어" 를 "맛있ㅓ" 로 그리는 실패가 자주 난다. 절대 그러지 마.)
- 자신 없는 글자가 있으면 **그 메모를 통째로 빼고** 그 자리에 하트나 반짝임 같은
  마크만 그려줘. 반쪽짜리 한글을 억지로 그리는 것보다 메모 하나 적은 편이 낫다.
- **메모를 뺄 때는 그 메모를 가리키던 화살표와 점선도 반드시 함께 뺀다.**
  가리킬 글이 없는 화살표가 허공을 가리키고 있으면 카드가 미완성으로 보인다.
  화살표는 메모가 있을 때만 그린다.
- 한 줄을 짧게 유지한다. 길수록 깨진다. 줄바꿈해서라도 짧게.
- 원본 사진의 비율을 유지한다. 자르거나 왜곡하지 마.
- 반려동물과 사람의 얼굴을 글씨나 장식으로 가리지 마.
{tail}"""

_GUARD_PHOTO = """- **원본 사진을 다시 그리지 마.** 피사체의 형태·색·질감, 배경, 색감을 그대로 두고
  그 위에 손글씨와 선만 덧그려줘.
- 사진에 없는 사물·동물·사람을 새로 그려 넣지 마."""

_GUARD_PLATE = """- **배경은 완전한 검정 한 가지 색이다.** 회색 얼룩도, 사진의 잔상도, 흐릿한 형체도
  남아 있으면 안 된다. 나무 한 그루, 풀 한 포기도 그리지 마라.
- **흰색 말고 다른 색을 쓰지 마.** 손글씨도 화살표도 장식도 전부 흰색이다.
- 손글씨를 그림자나 테두리로 감싸지 마. 흰 획만 있으면 된다."""


def build(
    kind: PhotoKind,
    style: WritingStyle,
    text: CardText,
    *,
    place_name: str | None = None,
    date_text: str | None = None,
    ink_plate: bool = False,
) -> str:
    """이미지 편집 API 에 그대로 넘길 프롬프트 한 덩어리.

    `ink_plate` 가 참이면 사진이 아니라 **검은 바탕의 손글씨 레이어**를 요구한다.
    """
    style_block = _STYLE_BASE.format(
        opening=_OPENING_PLATE if ink_plate else _OPENING_PHOTO,
        title_pen=_TITLE_PEN[style],
        title_avoid=_TITLE_AVOID[style],
        decorations=_DECORATIONS[style],
    )

    header_lines = []
    if place_name:
        header_lines.append(f"- 장소: {place_name}")
    if date_text:
        header_lines.append(f"- 날짜: {date_text}")
    header = ""
    if header_lines:
        header = "\n\n[상단 여백에 작은 손글씨로]\n" + "\n".join(header_lines)

    # 번호를 붙이지 않는다. 붙이면 모델이 그 번호까지 글자로 그린다(실측).
    # 괄호 안은 화살표를 어디로 그을지(또는 긋지 말지)를 정해 준다.
    memo_block = "\n".join(
        f"  · {m.text}"
        + (f"   (가리킬 것: {m.target})" if m.target else "   (화살표 없음)")
        for m in text.memos
    )
    guard = _GUARD.format(
        memo_count=len(text.memos),
        tail=_GUARD_PLATE if ink_plate else _GUARD_PHOTO,
    )

    return (
        f"{style_block}\n\n"
        f"[이 사진에 대해]\n{_KIND_HINTS[kind]}"
        f"{header}\n\n"
        f"[제목 — 크고 굵게 한 번만]\n  {text.title}\n\n"
        f"[메모 — 작고 얇게, 사진의 빈 곳에 나눠서]\n{memo_block}\n\n"
        f"{guard}"
    )
