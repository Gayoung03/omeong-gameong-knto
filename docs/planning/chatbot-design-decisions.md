# 챗봇 설계 결정

작성일: 2026-08-27 · 갱신: 2026-08-28 · 상태: **확정 31건 / 검색 도구 3개 구현 완료**

[`docs/api/chatbot.md`](../api/chatbot.md)는 **엔드포인트와 통신 형식**을 정한 문서입니다.
이 문서는 그 위에서 **어떤 챗봇을 만들 것인가**를 정합니다.

---

## 요약

| 구분 | 내용 |
| --- | --- |
| 무엇 | 제주 반려동물 여행 **장소 추천 + 준비물 안내** 챗봇 (캐릭터: 혼디) |
| 검색 방식 | **도구 호출 기반 SQL 검색** — 임베딩·pgvector 쓰지 않음 |
| LLM 벤더 | **OpenAI** (저렴한 소형 모델로 시작) |
| 쓰기 동작 | **없음.** 장소 저장은 답변 속 카드의 버튼으로 사용자가 직접 |
| 응답 방식 | SSE 스트리밍 |

**진행 상황** — 대화 CRUD 6개 엔드포인트는 구현·테스트 완료했습니다.
검색 도구를 막고 있던 지역·카테고리·태그 어휘는 **DB 실제 값으로 확인**돼
[4장](#4-어휘--db-에서-확인한-실제-값)으로 옮겼습니다. 남은 것은 데이터 품질 안건과
[중지 버튼](#3-중지-버튼--명세-확정-조항을-다시-열어야-합니다)입니다.

---

## 0. 검색 방식을 도구 호출로 정한 이유

[`README.md`](../api/README.md)의 미정 항목 **"챗봇 RAG 검색 방식"**이 이걸로 해소됩니다.

GPT는 우리 `places` 테이블을 볼 수 없습니다. 그래서 답변 전에 우리가 먼저 장소를 찾아
재료로 건네줘야 하는데(= RAG), 그 **찾는 방법**으로 두 가지가 있었습니다.

| | 도구 호출 (채택) | 벡터 검색 |
| --- | --- | --- |
| 방법 | GPT가 `search_places(지역, 카테고리, 동반정책)`을 호출 → 우리가 SQL로 조회 | 장소 설명을 임베딩해두고 질문과 가까운 것 검색 |
| 강점 | 조건이 **정확히** 맞음 | 뉘앙스 질문("조용한 곳")에 강함 |
| 새로 만들 것 | 함수 하나 | 임베딩 배치, `vector` 컬럼 마이그레이션, 재색인 작업 |

**결정적인 이유** — 질문 `"강아지 실내 동반 되는 제주 동부 카페"`에서

- 도구 호출: `policy = 'indoor_allowed'`로 걸러 **실내 동반 되는 곳만** 나옵니다
- 벡터 검색: `"강아지 실내 동반 가능"`과 `"반려견 출입 불가"`는 문장이 거의 같아
  임베딩상 매우 가깝습니다. **동반 불가 카페가 섞여 나옵니다**

반려동물 앱에서 이건 검색 오류가 아니라 **챗봇이 "여기 데려가세요"라고 틀린 확신을 주는
사고**입니다. 사용자가 개를 데리고 갔다가 문 앞에서 돌아섭니다.

여기에 더해, 우리 데이터는 이미 `category`·`region`·`pet_policy_type`·`tags` 컬럼으로
나뉘어 있고, 필터 표현식은 [`app/services/place_query.py`](../../apps/api/app/services/place_query.py)에
전부 구현돼 있습니다.

> **pgvector는 나중에.** 리뷰 본문처럼 진짜 자연어 덩어리가 쌓이면 그때 얹습니다.
> `infra/docker-compose.yml`이 이미 `pgvector/pg16` 이미지라 문은 열려 있습니다.

---

## 1. 확정 사항

### A. 챗봇의 정체

| # | 항목 | 결정 |
| --- | --- | --- |
| A1 | 범위 | **장소 추천 + 준비물 안내.** 날씨는 제외(별도 API 있음) |
| A2 | 쓰기 동작 | **없음.** 챗봇은 저장하지 않고, 답변 속 장소 카드에 저장 버튼을 답니다 |
| A3 | 제주 밖 질문 | **정중히 거절** — "제주 여행만 도와드릴 수 있어요" |
| A4 | 무관한 잡담 | **짧게 받아주고 여행 이야기로 유도** |
| A5 | 건강·의료 질문 | **답하지 않는다.** 수의사에게 안내하고 동물병원 검색을 제안. 단 탑승·반입 **서류와 절차는 규정**이라 안내한다 |
| A6 | 이동수단 규정 | **도구로 찾아 안내하고 확인일을 밝힌다.** 판정은 도구가 계산한 값을 그대로 쓴다 |
| A7 | 미확인 정보 | **단정하지 않는다.** 도구 결과에 없으면 "확인되지 않았다"고 말한다 |
| A8 | 견종 제한 | **되는지 안 되는지 어느 쪽도 단정하지 않는다.** 특정 견종을 맹견·단두종으로 분류하지 않는다 |

**A2가 중요합니다.** 챗봇이 직접 저장하면 GPT가 의도를 오해해 엉뚱한 장소를 저장할 수
있는데, 버튼 방식이면 그 사고가 원천적으로 없습니다. **챗봇은 완전히 읽기 전용입니다.**

**A5 — 건강·의료는 범위 밖입니다.** "약 먹여도 되나요"만 막으면 부족합니다. 멀미·스트레스
대처처럼 **여행과 붙어 있는 건강 질문**이 실제로 더 많이 들어오고, 이쪽이 더 위험합니다.
거절만 하면 막힌 느낌을 주므로, 동물병원 검색은 실제로 되는 기능이니 그쪽으로 돌립니다.

**A6이 풀렸습니다(2026-08-28).** `search_transport_rules` 와 `search_guides` 를 붙여
`transport_pet_rules` 12건과 `guide_documents` 15편을 챗봇이 직접 읽습니다.
답변에는 `verified_at` 을 "2026년 8월 확인 기준"으로 붙이고, 규정이 바뀔 수 있으니
예약 전 확인을 권합니다.

**무게 판정은 파이썬이 합니다.** `12 > 7` 같은 비교를 GPT 에게 맡기지 않습니다 —
모델은 숫자 비교를 곧잘 틀리고, 여기서 틀리면 **공항에서 탑승을 거부당합니다.**
`guide_search._verdict()` 가 `가능 / 불가 / 무게 초과 / 가능하나 무게 기준 미확인 /
확인 안 됨` 다섯 중 하나를 내고, GPT 는 그 결론을 문장으로 옮기기만 합니다.

**`확인 안 됨` 을 `불가` 로 뭉개지 않습니다.** `TransportPetRule` 의 boolean 이 전부
nullable 인 이유와 같습니다 — 원문이 「불가」라고 밝힌 것과 우리가 확인하지 못한 것은
다릅니다. 뭉개면 없는 규정을 만들어 답하게 됩니다.

**A8 — 부분 목록이 목록 없음보다 위험합니다.** `transport_restricted_breeds` 테이블은
있지만 **데이터가 비어 있습니다.** 수집한 가이드 본문에는 견종이 예시로만 적혀 있고
("퍼그·시추·불독 **같은**", "도사견·핏불테리어 **등**"), `notes` 에는 "단두종은 위탁 불가"
라는 분류만 있어 견종 이름 자체가 없습니다.

예시만 채워 도구에 붙이면 **"우리 애 복서인데요?" 에 "가능합니다"라고 답합니다** —
복서도 단두종인데 목록에 없기 때문입니다. 목록이 아예 없으면 "확인하세요"라고 하는데,
부분 목록이 있으면 확신을 갖고 틀립니다. `is_example_only` 플래그를 모델에 만들어 둔
이유이기도 합니다. **전체 목록을 수집하기 전까지는 프롬프트로 막습니다.**

**한쪽만 막으면 반대쪽으로 샙니다(2026-08-28 실측).** 처음에는 "제한이 없다고 말하지
말라"만 넣었는데, 화면에서 `"우리 애 복서인데 위탁 돼?"` 를 물으니 **"복서는 허용되지
않는 맹견으로 분류됩니다"** 라고 답했습니다. 복서는 우리 데이터 어디에도 없고
동물보호법상 맹견 5종에도 없습니다. 결론이 보수적이라 덜 위험해 보이지만 **없는 규정을
만들어 답한 것은 같고**, 실제로는 탈 수 있는 아이를 못 탄다고 한 것입니다.
규칙을 **양방향**으로 고쳤습니다 — 특정 견종을 분류하는 것 자체를 막습니다.

**A7은 A6이 풀린 뒤에도 남습니다.** 규정 데이터에는 빈 칸이 있습니다(요금·소요시간 일부는
수집 때 미확인으로 남겼습니다). 빈 칸을 GPT 가 그럴듯하게 메우지 않도록 하는 규칙입니다.

### B. 검색 도구

| # | 항목 | 결정 |
| --- | --- | --- |
| B3 | 도구 개수 | **3개** — `search_places`, `search_guides`, `search_transport_rules` |
| B4 | 도구 필터 | 지역·카테고리·동반정책·실내외·**태그·편의시설**·정렬·개수 |
| B5 | 반환 개수 | **5개** |
| B6 | 장소당 정보 | 이름·카테고리·지역·동반정책·평점 **+ 설명 80자** |
| B7 | 결과 0건 | **없다고 말하고 되묻기** — "애월에는 없네요. 근처 지역도 찾아볼까요?" |

**준비물은 새 테이블을 만들지 않습니다.** 여행 가이드 테이블을 읽는 도구만 붙입니다.
콘텐츠가 두 벌이 되지 않습니다.

**B3을 2개에서 3개로 늘렸습니다(2026-08-28).** 가이드 데이터가 성격이 다른 두 벌이기
때문입니다 — `guide_documents` 는 사람이 **읽는 글**(준비물·입도 절차·렌터카)이고,
`transport_pet_rules` 는 **숫자로 거르는 값**(무게 상한·요금·신청 기한, 컬럼 22개)입니다.
`"12kg 인데 기내 되나요"` 는 숫자 비교라 글 검색으로는 답이 나오지 않습니다.
하나로 합치면 GPT 가 언제 무게를 넘겨야 하는지 헷갈려 합니다.

**B6에서 설명을 80자로 자르는 이유** — `description` 전문을 5개 넣으면 입력 토큰이
폭증합니다. 80자면 GPT가 "왜 이 곳을 추천하는지" 쓸 근거로는 충분합니다.

**B6에 `id`는 당연히 포함됩니다** — `referencedPlaces`에 실을 UUID입니다.

### C. 답변 생성

| # | 항목 | 결정 |
| --- | --- | --- |
| C1 | 모델 | **저렴한 소형 모델로 시작.** 품질이 부족하면 모델명만 바꿔 올립니다 |
| C2 | 대화 맥락 | **최근 10턴** |
| C3 | 답변 길이 | **3~4문장, 장소는 최대 3곳** |
| C4 | `referencedPlaces` | **GPT가 답변에서 실제 언급한 장소만** |
| C5 | 지도 표시 | **서버가 판단** — 장소가 있으면 지도, 없으면 글만 |
| C6 | `routeId` 대화 | **1차에서 제외.** `route_id`는 항상 `null` |

**C1 — 소형 모델로는 부족한 사례가 나왔습니다(2026-08-28, 회의 안건).**

`gpt-4o-mini` 로 `"12kg 강아지 제주공항 기내 가능해?"`(회사를 안 집은 질문)를 물으면
**위탁을 세 번 연속 틀렸습니다.** 7곳 중 제주항공·티웨이·이스타는 위탁 제도가 없는데
`"모두 화물칸에 실을 수 있다"` 로 묶었습니다.

고치려고 세 가지를 순서대로 시도했습니다.

| 시도 | 방법 | 결과 |
| --- | --- | --- |
| 1 | 프롬프트에 "모두 ~하다로 묶지 말라" 추가 | 그대로 묶음 |
| 2 | 도구가 회사를 미리 분류해서 전달 | `위탁 제도 없음` 을 **`기내 탑승 불가`로 뒤집어** 적음 |
| 3 | 도구가 회사별 **결론 문장을 통째로** 전달 | 다시 `"위탁은 가능합니다"` 로 뭉갬 |

3번에서 모델이 받은 값은 `"기내 불가(상한 9kg 초과), 위탁 제도 없음 → 이 회사로는 이
무게로 갈 수 없음"` 입니다. **문장이 완성된 채로 들어갔는데도 반대로 썼습니다.**
`gpt-4o` 로 올리자 한 번에 정확해졌습니다 — 위탁 가능한 4곳만 정확히 골랐고, 시키지
않은 단두종 주의와 확인일까지 붙였습니다.

**기본값은 아직 `gpt-4o-mini` 로 둡니다.** 비용이 오르는 결정이라 팀이 정해야 합니다
([6장](#6-다음-결정이-필요한-것)). 도구 호출로 여러 건을 돌려주는 질문에서 소형 모델이
요약하다 틀린다는 것이 이 안건의 근거입니다 — 데이터와 프롬프트 쪽으로 할 수 있는
것은 위 세 가지로 다 해봤습니다.

**C2에 상한이 필요한 이유** — 매 질문마다 지금까지의 대화를 통째로 다시 보냅니다.
상한이 없으면 ① 늦게 묻는 질문일수록 비싸지고 ② 언젠가 모델 입력 한도를 넘어
**챗봇이 에러만 뱉기 시작합니다.** 여행 대화는 보통 5~10턴이라 체감 차이는 거의 없습니다.

**C4 구현** — 검색된 5개 중 **답변 텍스트에 이름이 등장한 것만 거릅니다.**
GPT에 재질의할 필요 없이 문자열 매칭으로 끝납니다.

**C5** — 앱의 키워드 판단([`chatbotMapResponse.ts`](../../apps/mobile/src/features/chatbot/utils/chatbotMapResponse.ts))은
삭제합니다. 장소를 못 찾았는데 "카페"라는 단어만 보고 빈 지도가 뜨는 일이 없어집니다.

**C6** — DB 컬럼과 API 필드는 그대로 둡니다. 나중에 켤 수 있습니다.

### D. 대화 관리

| # | 항목 | 결정 |
| --- | --- | --- |
| D1 | 제목 자동 생성 | **첫 질문 앞 30자 자르기.** LLM 호출 0회 |
| D2 | 대화 개수 상한 | **100개, 넘으면 생성 거부** ("안 쓰는 대화를 지워주세요") |
| D3 | 보관 기간 | **지우지 않음.** 사용자가 직접 지울 때만 |
| D4 | `lastMessagePreview` | **20자** |

**D2를 자동 삭제로 안 한 이유** — 사용자가 저장해 둔 대화를 예고 없이 지우게 됩니다.
100개까지 채우는 사람은 챗봇을 아주 많이 쓰는 사용자인데, 하필 그 사람 것을 지웁니다.
번거로움과 데이터 유실 중에서는 번거로움이 낫습니다.

### E. 실패·한계·비용

| # | 항목 | 결정 |
| --- | --- | --- |
| E1 | 에러 문구 | **서버가 보낸 `detail`을 앱이 그대로 표시** |
| E2 | 타임아웃 | **첫 조각 20초 / 전체 60초** |
| E3 | 사용량 제한 | **하루 15회.** `ENVIRONMENT=local`이면 무제한 |
| E4 | 재시도 | **서버 자동 재시도 없음.** 앱의 재시도 버튼만 |
| E5 | 부적절한 질문 | OpenAI 필터 + **시스템 프롬프트에 규칙도 명시** |

**E1이 [`README.md`](../api/README.md)의 미정 항목 "에러 메시지 언어"를 해소합니다.**
[`chatbot.md`](../api/chatbot.md)의 에러 이벤트가 이미 `code`와 `detail`을 **둘 다** 내려주므로
**명세는 바꾸지 않습니다.** 앱이 `detail`을 쓰기로 한 것뿐이고, 나중에 특정 에러만
앱에서 꾸미고 싶어지면 그때 `code`로 분기하면 됩니다.

문구를 고치려고 앱스토어 심사를 기다리지 않아도 되는 게 이 선택의 이유입니다.

**E3** — [`config.py`](../../apps/api/app/core/config.py)에 `environment` 설정이 이미 있어
개발·테스트는 제한 없이, 운영만 하루 15회로 갑니다.

### F. 앱 화면

| # | 항목 | 결정 |
| --- | --- | --- |
| F1 | 대화 목록 화면 | **만든다.** 대화 목록 + 대화 상세, 화면 2개 추가 |
| F2 | 재진입 시 | **항상 새 대화.** 지난 대화는 목록에서 찾아 들어갑니다 |
| F3 | 스트리밍 중 | 깜빡이는 커서 + 입력창 잠금 + **중지 버튼** |
| F4 | 추천 질문 칩 | **"주변 동물병원이 어디에 있어?" → "제주시에 반려동물 병원 알려줘"** |

**F1로 엔드포인트 7개가 전부 살아납니다.** 명세를 축소할 필요가 없어졌습니다.

**F4가 필요한 이유** — 개인위치정보를 수집하지 않기로 확정했으므로 "주변"을 알 수 없습니다.
칩 하나가 **답할 수 없는 질문**을 사용자에게 권하고 있었습니다.

---

## 2. 결정에서 파생된 규칙

### 대화는 탭을 열 때가 아니라 첫 질문을 보낼 때 만듭니다

F2("항상 새 대화")와 D2("100개 상한")가 그냥 두면 부딪힙니다.
탭을 열 때마다 대화를 만들면 **질문도 안 한 빈 대화가 쌓여 100개가 금방 찹니다.**

→ 챗봇 탭은 빈 화면 + 추천 질문 칩으로 시작하고, **첫 질문을 보내는 순간 대화가 생성**됩니다.
[`chatbot.md`](../api/chatbot.md)의 `POST /chat/conversations`가 `firstMessage`를 함께 받게
돼 있어 **명세 변경 없이 됩니다.**

---

## 3. 중지 버튼 — 명세 확정 조항을 다시 열어야 합니다

F3에서 중지 버튼을 넣기로 했고, **중지하면 그때까지의 답변을 저장**하기로 했습니다.
[`chatbot.md`](../api/chatbot.md)의 2026-08-18 확정 조항 **두 개와 충돌**합니다.

| 현재 명세 | 필요한 변경 |
| --- | --- |
| "중간에 끊기면 저장하지 않습니다" | 사용자가 중지한 경우는 **예외로 저장** |
| "답변 생성 중지 버튼은 이번 범위에 없습니다" | 범위에 **포함** |

### 그런데 서버는 둘을 구분할 수 없습니다

사용자가 "그만"을 눌러도, 지하철에서 신호가 끊겨도,
서버 입장에서는 **똑같이 "클라이언트가 연결을 끊었다"**입니다.

```
사용자가 중지 버튼 누름   →  연결 끊김  →  저장해야 함
지하철에서 신호 끊김      →  연결 끊김  →  저장하면 안 됨
                             ↑ 서버는 이 둘이 같아 보입니다
```

**팀이 중지 버튼을 미룬 이유가 정확히 이겁니다** — 명세에도 "중단 신호를 보낼 방법과
'여기까지는 저장' 규칙이 함께 필요해서"라고 적혀 있습니다.

### 풀 수 있는 방법 (미결 — 팀 논의 필요)

| 방법 | 내용 | 걸리는 점 |
| --- | --- | --- |
| ⓐ 앱이 저장을 요청 | 중지 시 앱이 받은 텍스트를 서버로 보내 저장 요청 | 엔드포인트 1개 추가. 앱이 보낸 값을 믿어야 함 |
| ⓑ 중단 신호 엔드포인트 | `POST .../messages/stop`으로 "지금 중지" 신호 | 엔드포인트 1개 추가. 신호와 연결 끊김의 순서 경합 |
| ⓒ 끊기면 무조건 저장 | 구분을 포기하고 항상 저장 | 구현이 가장 쉽지만, 명세가 막으려던 "잘린 답변이 기록에 남는 문제"가 돌아옴 |

**이 결정은 이 문서에서 하지 않습니다.** 챗봇 명세 회의 안건입니다.

---

## 4. 어휘 — DB 에서 확인한 실제 값

> **2026-08-27 갱신 — 세 건 모두 DB 에서 실제 값을 확인했습니다.**
> 문서마다 표기가 달라 "정해야 할 것"으로 보였지만, **운영 데이터는 이미 한 벌로
> 돌아가고 있었습니다.** 회의에서 새로 정할 것은 아래 [남은 안건](#남은-안건)뿐입니다.

### B1. 지역 어휘 — 관광권역 6개

장소 1299건의 `region` 실제 분포입니다.

| 권역 | 장소 수 |
| --- | --- |
| 제주시/제주국제공항 | 677 |
| 애월/한림/협재 | 279 |
| 함덕/김녕/세화 | 159 |
| 표선/성산 | 111 |
| 서귀포시/모슬포 | 42 |
| 중문 | 27 |
| ~~제주시~~ · ~~서귀포시~~ | 3 · 1 — `seed_dev.py` 잔여물 |

**스키마 변경이 필요 없습니다.** `places.region` 은 이 권역 문자열을 그대로 두고,
GPT 에게 권역 목록과 방위 매핑을 시스템 프롬프트로 줍니다.

| 방위 | 권역 |
| --- | --- |
| 동부 | 함덕/김녕/세화, 표선/성산 |
| 서부 | 애월/한림/협재 |
| 남부 | 중문, 서귀포시/모슬포 |
| 제주시권 | 제주시/제주국제공항 |

### B2. 카테고리 어휘 — 12종, 다만 `etc` 가 21%

```
cafe 526 · etc 278 · attraction 149 · accommodation 148 · pet_service 82
restaurant 46 · beach 21 · restaurant_cafe 15 · oreum 12
veterinary_hospital 11 · walking_trail 8 · rental_experience 3
```

**`etc` 278건(21%)은 챗봇이 카테고리로 찾을 수 없습니다.** 다섯 곳 중 한 곳입니다.
`restaurant` 와 `restaurant_cafe` 도 겹칩니다. 둘 다 검색 품질을 직접 깎으므로
장소 데이터 회의 안건으로 넘깁니다.

앱 목업 `place.mock.ts` 는 여전히 한글(`카페·식당`)이라 **팀 규약("값은 영문 코드")
위반 상태**입니다.

### `place_tags` 목록 — 7종, 이미 영문 코드

```
sea · cafe · walk · photo_spot · experience · rest · indoor_tourism
```

`app/recommend/config/tags.py` 의 한글 `STANDARD_TAGS`(바다·카페·산책·포토스팟·
체험·휴식·실내관광)와 **1:1 로 맞아떨어집니다.** 추천 엔진은 한글 라벨을,
DB 는 영문 코드를 쓰고 있을 뿐 같은 7종입니다.

분위기 질문(“조용한 곳”, “사진 예쁜 데”)을 받아내는 것도 결국 이 태그입니다 —
`rest`, `photo_spot`, `sea` 로 번역하는 일은 GPT 가 합니다.

### 동반정책 `unknown` 은 "모름"이 아니라 "세부 미확인"입니다

카카오 데이터는 **동반 가능한 곳만 골라 수집**했습니다. 그래서 `unknown` 은
"동반이 되는지 모른다"가 아니라 **"동반은 되는데 실내까지인지 야외만인지 모른다"**
입니다. 라벨도 앱에서 '정보 없음' → **'동반 가능 · 세부 미확인'** 으로 바꿨습니다.

이게 검색 규칙을 바꿉니다. 장소 대부분이 `unknown` 이라, "강아지랑 갈 수 있는
카페"에 `pet_policy=indoor_allowed` 를 걸면 대부분이 걸러집니다.

| 지역 | 카페 | `indoor_allowed` 명시 |
| --- | --- | --- |
| 제주시/제주국제공항 | 248 | 3 |
| 애월/한림/협재 | 127 | 1 |
| 함덕/김녕/세화 | 93 | 3 |
| 표선/성산 | 47 | 1 |

**그래서 `pet_policy` 는 기본적으로 넘기지 않습니다.** 넘기지 않으면 도구가
`not_allowed` 만 빼고 나머지를 다 봅니다. 사용자가 실내·야외를 구분해 물을 때만
씁니다. 이 규칙은 `app/rag/prompts/system.py` 의 동반정책 절에 들어가 있습니다.

### 검색 대상 장소를 667 → 1268 로 늘렸습니다 (2026-08-28)

`is_active=false` 로 들어와 있던 카카오 장소 **601건**을 켰습니다. 8/12 임포트가
그렇게 넣었을 뿐 끈 흔적이 없어, 담당자 확인 후 켜기로 했습니다.

| 카테고리 | 켠 것 | 켠 뒤 총계 |
| --- | --- | --- |
| cafe | 504 | 518 |
| restaurant | 44 | 45 |
| accommodation | 31 | 144 |
| pet_service | 22 | 66 |

켜는 일은 `apps/api/scripts/activate_kakao_places.py` 가 합니다. 일회성 SQL 로
처리하면 나중에 "장소가 왜 601개 늘었지?" 를 아무도 못 쫓기 때문입니다.

카카오 장소 활성화와 확인된 숙소 오분류 4건을 함께 반영할 때는 통합 정정
스크립트를 사용합니다. 기본 실행은 확인만 하며 `--apply`를 붙여야 반영합니다.

```bash
cd apps/api && uv run python -m scripts.repair_place_data
cd apps/api && uv run python -m scripts.repair_place_data --apply
```

```bash
cd apps/api && uv run python -m scripts.activate_kakao_places          # 확인만
cd apps/api && uv run python -m scripts.activate_kakao_places --apply  # 실제로 켜기
```

이미 켜진 것은 빠지므로 여러 번 돌려도 안전하고, 공유 DB 에는 한 번 더 묻습니다.
켠 장소 id 는 파일로 남아 `--revert` 로 되돌릴 수 있습니다.

**적용 현황**

| DB | 상태 |
| --- | --- |
| 로컬 | 601건 적용 완료 |
| 공유 RDS | 미적용 — 켜는 순간 팀원 앱의 장소 목록에도 601건이 나타납니다 |

꺼둔 채로 남긴 31건은 아래와 같습니다.

| 대상 | 건수 | 왜 남겼나 |
| --- | --- | --- |
| 카카오 + kcisa | 28 | 8/15 에 누군가 수정한 흔적이 있어 의도를 확인해야 함 |
| visitjeju 단독 | 3 | 카카오가 아니라 승인 범위 밖 |

### 남은 안건

| 안건 | 왜 |
| --- | --- |
| `etc` 278건 분류 | 카테고리 검색에 안 걸림 |
| `restaurant` / `restaurant_cafe` 중복 정리 | 같은 것을 둘로 나눠 검색이 새어나감 |
| `제주시`·`서귀포시` 잔여 4건 정리 | 권역 목록에 없는 값 |

> **챗봇만의 문제가 아닙니다.** [`places.md`](../api/places.md) 의 장소 검색이 같은 값을
> 쓰므로 **장소 데이터 회의**에서 다룹니다.

---

## 5. 무엇을 먼저 만들 수 있나

```
[완료]       대화 CRUD 6개 엔드포인트  ─── LLM도 검색도 필요 없음
                    ↓
[다음]       search_places 도구        ─── 어휘 확인으로 막힘이 풀림
                    ↓
[디자인 대기] 앱 타입 정리 + 대화 목록·상세 화면 (F1)
                    ↓
[가이드 테이블 대기] search_guides 도구
                    ↓
[명세 회의 대기] 스트리밍 엔드포인트 + 중지 버튼
```

**앱 화면(F1)을 검색 도구보다 뒤에 둔 이유** — 디자인 시안이 없어 지금 만들면
시안이 나온 뒤 다시 만들 가능성이 큽니다. 검색 도구는 막힌 것이 없고,
만들다 보면 `etc` 278건이 실제로 얼마나 아픈지 수치로 나와 장소 데이터 회의의
근거가 됩니다.

---

## 6. 다음 결정이 필요한 것

| 안건 | 어디서 정할 것인가 |
| --- | --- |
| `etc` 278건 분류 · `restaurant`/`restaurant_cafe` 중복 · 잔여 4건 | 장소 데이터 회의 (`places.md`와 공동) |
| 중지 버튼의 중단 신호 방식 (ⓐ/ⓑ/ⓒ) | 챗봇 명세 회의 |
| 가이드 테이블 스키마 | 가이드 담당자 |
| **`OPENAI_MODEL` 을 `gpt-4o` 로 올릴지** (비용 증가 / 소형 모델은 위탁 답변을 세 번 틀림) | 챗봇 명세 회의 — 근거는 [C1](#c-답변-생성) |
| **견종 제한 전체 목록 수집** (대한항공 8종·아시아나 12종·진에어 10종·제주항공 15종 등) | 다음 작업 — 수집 후 `transport_restricted_breeds` 를 채우고 도구에 붙인다 |
| 분위기 질문에 되물을지 / 못 한다고 말할지 | **검색 도구를 만들어 써본 뒤** |

### 분위기 질문은 지금 벡터로 바꿔도 해결되지 않습니다

"조용하고 분위기 좋은 카페" 같은 질문이 도구 호출 SQL 의 약점인 것은 맞습니다.
그런데 **지금 우리 데이터로는 벡터 검색도 똑같이 답하지 못합니다.**

| | 상태 |
| --- | --- |
| `description` 있는 장소 | 548 / 1299 (42%) |
| 평균 길이 | 52자 — 100자 넘는 것은 **2건** |
| 내용 | 분위기 서술이 아니라 `[KCISA] 강아지미용, 호텔 …` 분류 코드 덤프 |
| 리뷰 | **0건** |

벡터 검색은 글의 뜻을 찾는 기술이지 **없는 정보를 만들어내지는 못합니다.**
임베딩할 산문이 아직 없으므로, 지금 붙이면 파이프라인만 생기고 품질은 그대로입니다.

지금 받아낼 수 있는 만큼은 **태그 7종**입니다 — "조용한 곳"→`rest`,
"사진 예쁜 데"→`photo_spot`, "탁 트인 곳"→`sea`. 번역은 GPT 가 합니다.

**벡터를 붙일 시점은 리뷰가 쌓였을 때**입니다. 사용자 후기가 진짜 분위기
데이터이고, 그때 재설계 없이 얹을 수 있습니다.

## 7. 이 문서가 해소하는 기존 미정 항목

[`README.md`](../api/README.md) 8장 미정 항목 표에서 두 건이 정리됩니다.

- [x] **챗봇 RAG 검색 방식** — 도구 호출 기반 SQL 검색 ([0장](#0-검색-방식을-도구-호출로-정한-이유))
- [x] **에러 메시지 언어** — 서버가 한국어 `detail`을 내려주고 앱이 그대로 표시 ([E1](#e-실패한계비용))

## 8. 확정 시 따라오는 문서·코드 수정

| 대상 | 수정 내용 |
| --- | --- |
| [`chatbot.md`](../api/chatbot.md) | `"modelName": "claude-sonnet-5"` 예시를 OpenAI 모델명으로 |
| [`chatbot.md`](../api/chatbot.md) | 중지 버튼 관련 확정 조항 2개 재작성 ([3장](#3-중지-버튼--명세-확정-조항을-다시-열어야-합니다)) |
| [`README.md`](../api/README.md) | 미정 항목 표에서 2건 제거 |
| [`guides.md`](../api/guides.md) | "관련 DB 테이블: 없음" — 실제 작업보다 뒤처져 있음 |
| [`places.md`](../api/places.md) | `region`·`category` 허용값을 [4장](#4-어휘--db-에서-확인한-실제-값)의 실제 값으로 명시 |
| 앱 `place.mock.ts` | 한글 카테고리를 영문 코드로 (규약 위반 상태) |
| 앱 `chatbotSuggestions.ts` | 동물병원 칩 문구 변경 ([F4](#f-앱-화면)) |
| 앱 `chatbotMapResponse.ts` | 키워드 기반 지도 판단 삭제 ([C5](#c-답변-생성)) |
| 앱 `types/chatbot.ts` | `id: number` → UUID 문자열 등 4곳 ([`chatbot.md`](../api/chatbot.md) 확정 #6) |

---

## 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-08-27 | 초안 작성. 검색 방식(도구 호출)·LLM 벤더(OpenAI) 확정, 설계 항목 31건 중 28건 확정·3건 보류. 중지 버튼 도입에 따른 명세 재검토 안건 제기 |
| 2026-08-28 | 활성화를 `scripts/activate_kakao_places.py` 로 옮김(재실행 안전·되돌리기 지원) |
| 2026-08-28 | 카카오 장소 601건을 로컬에서 활성화(검색 대상 667→1268). `unknown` 의 뜻을 "세부 미확인"으로 확정하고, 그에 맞춰 `pet_policy` 를 기본적으로 넘기지 않도록 프롬프트 수정 |
| 2026-08-27 | 대화 CRUD 6개 구현 완료. **지역·카테고리·태그 어휘를 DB 실제 값으로 확인**해 보류에서 해제(4장) — 남은 것은 `etc` 278건 등 데이터 품질 안건. 분위기 질문에 벡터가 지금은 답이 아닌 근거 추가 |
| 2026-08-28 | **답변 가이드라인 3건 추가(A5·A6·A7).** 회의에서 정했던 수의학 답변 금지가 `system.py` 에 한 줄도 없었다 — 지금 상태로 "멀미약 먹여도 되나요"에 GPT 가 그냥 답한다. 함께 확인하다 **이동수단 규정도 같은 구멍**인 것이 드러났다: `transport_pet_rules` 에 규정을 다 넣어두고도 읽는 도구가 없어 GPT 가 지어낸다. **A6 은 도구가 붙을 때까지만 유효한 한시 규칙**이고, `search_guides` 작업에서 "출처·확인일을 붙여 안내"로 교체한다. A7(미확인 단정 금지)은 원래 답변 원칙에 있었으나 문서·코드 어디에도 없었다 |
| 2026-08-28 | **`search_guides` · `search_transport_rules` 구현. A6 한시 규칙 해제.** 도구를 **2개에서 3개로 늘렸다**(B3) — 가이드 데이터가 읽는 글과 숫자로 거르는 값 두 벌이라, 하나로 합치면 GPT 가 언제 무게를 넘길지 헷갈려 한다. **무게 판정은 파이썬에서 한다**: `12 > 7` 을 모델에게 맡기면 틀리고, 여기서 틀리면 공항에서 탑승을 거부당한다. `확인 안 됨` 과 `불가` 를 판정에서도 구분해 남겼다 — `TransportPetRule` 의 boolean 이 nullable 인 이유와 같다. `_describe_rule` 은 **값이 없는 항목을 아예 빼서** 보낸다: `null` 을 넘기면 GPT 가 "없다 = 불가"로 읽는다. 확인하다 **A5(의료 금지)가 입도 서류 질문까지 막는** 것이 드러나 경계를 명시했다 — 어떤 접종을 맞힐지는 의료지만, 탑승·반입에 필요한 서류는 규정이다. **남은 것: `transport_restricted_breeds` 테이블에 데이터가 없다** — 견종 제한은 지금 `notes` 텍스트에만 있다(별도 안건) |
| 2026-08-28 | **A8(견종 제한) 추가.** `transport_restricted_breeds` 가 비어 있는 것을 확인하고 세 가지 안(예시만 넣기 / 전체 수집 후 넣기 / 프롬프트로 막기)을 따져 **프롬프트로 막는 쪽**을 골랐다. 예시만 넣으면 목록에 없는 견종을 "괜찮다"고 답하게 되는데, **부분 목록은 목록 없음보다 위험하다** — 없으면 확인을 권하지만 있으면 확신을 갖고 틀린다. 전체 목록 수집은 코드가 아니라 자료 조사라 브랜치를 나눠 다음 작업으로 잡았다(6장) |
| 2026-08-28 | **A8 규칙을 양방향으로 고침.** 화면 확인에서 `"복서인데 위탁 돼?"` 에 챗봇이 **"허용되지 않는 맹견으로 분류됩니다"** 라고 답했다 — 복서는 우리 데이터에도 동물보호법 맹견 5종에도 없다. 규칙이 `"괜찮다고 말하지 말라"` 한쪽만 막고 있어서 모델이 **반대 방향으로 지어냈다.** 보수적인 오답도 오답이다. 나머지 답변(제주항공 기내 9kg·위탁 없음, 대한항공 7kg/45kg, 입도 서류, 차멀미약 차단)은 데이터와 대조해 전부 정확했다 |
| 2026-08-28 | 양방향 규칙 적용 후 재확인 — `"복서인데 위탁 돼?"` 에 **분류하지 않고 "확인되지 않았다"** 로 답했다. 다만 **단두종·맹견 제한이 있다는 맥락까지 빠져** 사용자가 왜 확인해야 하는지 모른다. 맥락을 더 요구하면 다시 분류 쪽으로 흘러갈 여지가 있어 **이 상태로 둔다** — 견종 전체 목록을 수집하면(6장) 안전과 친절이 함께 풀린다 |
| 2026-08-28 | **RDS 확인에서 일반화 오답 발견 — 규칙 추가.** `"12kg 제주공항 기내 가능해?"`(회사를 안 집은 질문)에 7곳을 조회한 뒤 **"모든 항공사에서 화물칸에 실을 수 있다"** 고 답했다. **7곳 중 3곳(제주항공·티웨이·이스타)은 위탁이 아예 없다** — 12kg 이면 그 세 곳으로는 갈 수 없는데 그 사실이 지워졌다. 도구는 회사마다 `cargo_verdict` 를 정확히 보냈고 **모델이 요약하며 뭉갰다.** 로컬에서는 회사를 집어 물어 한 곳만 조회돼 걸리지 않았다 — **여러 건을 돌려주는 질문을 따로 확인해야 한다.** 함께 발견: `"애월에서 강아지랑 갈 수 있는 카페"`(추천 질문 칩에 있는 문구)가 RDS 에서 0건이다. 지역 어휘는 정상이므로 4장의 `restaurant`/`restaurant_cafe` 중복 안건이 원인으로 의심된다 — 장소 데이터 회의 안건 |
| 2026-08-28 | **`gpt-4o` 로 올려 위탁 오답이 해결됐다 — 다만 기본값은 그대로 둔다.** 세 번의 수정(프롬프트 / 도구 분류 / 회사별 결론 문장)으로도 `gpt-4o-mini` 가 `"위탁은 가능합니다"` 를 반복했다. 모델을 올리자 위탁 가능한 4곳만 정확히 골랐다. **비용이 오르는 결정이라 코드 기본값(`config.py` 의 `gpt-4o-mini`)과 `.env.example` 은 건드리지 않고 회의 안건으로 올린다**(6장) — 지금은 각자 `.env` 에서 바꿔 쓴다. C1 에 시도 3건과 결과를 기록했다 |
